#include "object.hpp"


Object::Object(sf::RenderWindow* window, int width, int height, int x, int y, sf::Texture* texture, int mass) : Entity(window, width, height, x, y, texture, mass){
	//todo
}

Object::~Object(){
	//todo
}