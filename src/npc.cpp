#include "npc.hpp"

NPC::NPC(sf::RenderWindow* window, int width, int height, int x, int y, std::map<int, std::map<int, Entity*>>* entitymap) : Creature(window, width, height, x, y, entitymap){
	//todo
}
NPC::~NPC(){
	//todo
}

