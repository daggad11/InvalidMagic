#include "npc.hpp"

NPC::NPC(sf::RenderWindow* window, int width, int height, int x, int y, sf::Texture* texture, int mass, std::map<int, std::map<int, Entity*>>* tilemap) : Creature(window, width, height, x, y, texture, mass, tilemap){
	//todo
}
NPC::~NPC(){
	//todo
}

