#include "npc.hpp"

NPC::NPC(sf::RenderWindow* window, int width, int height, int x, int y, int mass, std::map<int, std::map<int, Entity*>>* tilemap) : Creature(window, width, height, x, y, mass, tilemap){
	//todo
}
NPC::~NPC(){
	//todo
}

